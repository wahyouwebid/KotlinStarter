package coroutines.job_and_deffered.latihan1

import kotlinx.coroutines.*

fun main() = runBlocking {

    // menggunakan launch()
    val jobLaunch = launch {
        // do background task here }
    }

    // menggunakan job()
    val job = Job()
}